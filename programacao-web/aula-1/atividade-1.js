/*
    Faça a declaração de 2 variáveis numéricas para cada operador e guarde o resultado da operação em uma terceira variável;
    Para cada resultado obtido, imprima o texto “Resultado da {operação} entre {num_A} e {num_B} é {resultado}
*/

let xAdd = 3;
let yAdd = 6;

let xMm = 8;
let yMm = 4;

let xMult = 10;
let yMult = 5;

let xDiv = 60;
let yDiv = 8;

let add = xAdd + yAdd;
let minus = xMm - yMm;
let mult = xMult * yMult
let div = xDiv / yDiv

console.log(`Resultado da soma entre ${xAdd} e ${yAdd} é igual a ${add}`)
console.log(`Resultado da subtração entre ${xMm} e ${yMm} é igual a ${minus}`)
console.log(`Resultado da multiplicação entre ${xMult} e ${yMult} é igual a ${mult}`)
console.log(`Resultado da divisão entre ${xDiv} e ${yDiv} é igual a ${div}`)